<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <style>
        * {
            font-family: 'Times New Roman', serif;
            font-size: 26px;
            font-weight: normal;
        }
        h1{
            font-size: 56px;
            font-weight: bold;
        }
        .error{
            color: red;
        }
        a{
            font-size: 36px;
            color: black;
            text-decoration: none;
            margin-left: 20px;
            background-color: lightblue;
            padding: 0 5px;
        }
        a:hover{
            background-color: rgb(119, 171, 189);
        }
    </style>
</head>

<body>
    <a href="/">Home</a>
    <a href="/auth/login">Login</a>
    <a href="/auth/register">Register</a>
    <a href="/auth/logout">Logout</a>

    {{#if errors}}
    <div class="error">
        {{#each errors}}
        <p>{{this}}</p>
        {{/each}}
    </div>
    {{/if}}
    {{{body}}}
</body>

</html>